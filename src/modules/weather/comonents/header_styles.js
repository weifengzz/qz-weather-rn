import {
    StyleSheet
} from 'react-native'

const styles = StyleSheet.create({
    container: {
        alignItems: 'center'
    },
    titleText: {
        fontSize: 18,
        color: 'white',
        fontWeight: 'bold'
    },
    dateText: {
        color: 'white',
        marginTop: 10
    }
})

export default styles